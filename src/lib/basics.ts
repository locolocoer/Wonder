// 编译原理基础知识（面向零基础），作为独立栏目展示。
// content 为 Markdown；代码块用 ~~~ 围栏，行内代码用反引号（在模板字符串内转义为 \`）。

export interface BasicsSection {
  id: string;
  title: string;
  content: string;
}

export const BASICS: BasicsSection[] = [
  {
    id: 'what-is-compiler',
    title: '1. 编译器是什么',
    content: `**编译器**（compiler）本质是一个「翻译程序」：把人类能读懂的**源代码**，翻译成计算机能直接执行的**机器指令**。

打个比方：编译器就像一位翻译官，把「中文说明书」翻译成「机器听得懂的指令」。

~~~
源代码（C/C++）  ──编译器──▶  机器码 / 汇编
  int main(){              movl $42, %eax
    return 42;             ret
  }
~~~

**解释器 vs 编译器：**

- **编译器**：一次性把整个程序翻译成机器码，之后直接运行（快）。C/C++、Go、Rust 都是编译型。
- **解释器**：一边读源代码一边执行，不预先翻译（灵活）。Python、JavaScript 常见。

**本项目做的编译器**分两步翻译：

1. 你的程序（mycc）把「C 子集源代码」翻译成 **x86-64 汇编**（人能勉强看懂，机器还看不懂）；
2. 交给现成的 **gcc/g++**，把汇编再翻译成真正的机器码（可执行文件）。

所以你不必自己处理最底层的机器码，只要把汇编文本输出正确即可——这大大降低了难度。`,
  },

  {
    id: 'pipeline',
    title: '2. 编译的四个阶段（总览）',
    content: `编译器内部通常分两大块：

- **前端**：和「语言」打交道——读懂源代码、检查对错；
- **后端**：和「机器」打交道——生成目标机器的代码。

本项目的前端 + 后端拆成 **4 个阶段**（也是课程路线的划分依据）：

| 阶段 | 名称 | 输入 | 输出 | 在课程里 |
|---|---|---|---|---|
| ① | 词法分析 | 字符流 | token 序列 | 阶段 2 |
| ② | 语法分析 | token 序列 | 抽象语法树 AST | 阶段 3 |
| ③ | 语义分析 | AST | 带符号表的 AST | 阶段 4 |
| ④ | 代码生成 | AST | x86-64 汇编 | 阶段 5 |

举个具体例子，源代码 \`return 42;\` 会经历：

~~~
① 词法：  [KEYWORD 'return'] [NUM '42'] [PUNCT ';']
② 语法：  (return (num 42))
③ 语义：  (return (num 42))   ← 检查变量等，此处无变量，直接通过
④ 代码：  movl $42, %eax
           jmp .L.return
~~~

**为什么要分阶段？** 因为每阶段只解决一个问题，复杂度可控；任何一阶段出错都能精确定位。这就是「分而治之」。

> 记忆口诀：**词法看单词，语法看结构，语义看意义，代码生成看机器。**`,
  },

  {
    id: 'encoding',
    title: '3. 前置知识：字符与编码',
    content: `编译器处理的最底层单位是**字节（byte）**，一个字节是 0~255 的整数。

**ASCII**：最早的标准，用 1 字节表示一个字符。比如 'A'=65、'a'=97、'0'=48、空格=32、换行=10、回车=13。

**UTF-8**：现在主流的中文编码，用 1~4 字节表示一个字符。ASCII 字符在 UTF-8 里还是 1 字节（向后兼容），所以英文代码/关键字不受影响。

**换行符**：不同系统不一样——
- Windows：\`\\r\\n\`（回车 + 换行，两个字符：13、10）
- Linux/macOS：\`\\n\`（一个字符：10）

**词法器必须跳过空白字符**，包括空格(32)、制表符 Tab(9)、换行(10)、回车(13)。C 里可用 \`isspace()\`，C++ 里用 \`std::isspace()\` 判断。

**BOM（字节序标记）**：有些 Windows 编辑器在文件开头写入 3 个字节 \`EF BB BF\`。你的词法器要能跳过它，否则第一个 token 会解析错。

**重要提醒**：程序里判断字符时，注意 \`char\` 可能是有符号的，中文或高位字节会变成负数。安全做法是转成 \`unsigned char\` 再判断。`,
  },

  {
    id: 'lexer',
    title: '4. 词法分析 Lexer',
    content: `**词法分析**把字符流切成一个个**词法单元 token**——它是语言里不可再分的最小单元。

**token 的类型**（本项目用到）：

| 类型 | 例子 | 说明 |
|---|---|---|
| 关键字 KEYWORD | int、return、if、else、while、for | 语言保留字 |
| 标识符 IDENT | main、x、readFile | 变量/函数名 |
| 数字 NUM | 42、100 | 整数常量 |
| 字符串 STR | "hello" | 字符串字面量 |
| 运算符/标点 PUNCT | + - * / % ( ) { } ; = == != < <= > >= && \|\| | 运算符和标点 |
| 结束 EOF | （无） | 文件结尾标记 |

**实现思路**：用一根「指针」从左到右扫描字符，根据当前字符决定读什么：

~~~
while (还有字符) {
  跳过空白
  如果是数字   → 读完整数字，生成 NUM
  如果是字母/_ → 读完整标识符，查表判断是不是关键字
  如果是引号   → 读到下一个引号，生成 STR
  如果是 / 且后面是 / 或 * → 跳过注释
  如果是多字符运算符（先试 == != <= >= && ||）→ 生成 PUNCT
  否则是单字符标点 → 生成 PUNCT
}
最后补一个 EOF
~~~

**几个关键点：**

1. **最长匹配**：遇到 \`=\` 时先看下一个字符，若是 \`=\` 则合成 \`==\`，否则才是单个 \`=\`。多字符运算符必须先于单字符判断。
2. **关键字 vs 标识符**：先按标识符规则切出来，再查一张关键字表（int/return/if/else/while/for）判断。
3. **注释**：\`//\` 到行尾；\`/* ... */\` 跨行。要跳过，否则注释里的内容会被误当成代码。
4. **数值**：读连续数字，用 \`strtol\`（C）或 \`std::stoi\`（C++）转成整数。

**数据结构**：C 里用链表存 token（每个 token 一个节点 + next 指针）；C++ 里直接 \`std::vector<Token>\` 即可，用下标遍历。`,
  },

  {
    id: 'parser',
    title: '5. 语法分析与 AST',
    content: `**语法分析**检查 token 的顺序是否符合语法规则，并把它组织成一棵**抽象语法树 AST**。

**AST（Abstract Syntax Tree）**：每个语法结构一个节点。比如 \`return 1 + 2;\` 的 AST：

~~~
       (return)
          |
        (bin +)
        /     \\
     (num 1) (num 2)
~~~

用 S 表达式表示就是：\`(return (bin + (num 1) (num 2)))\`。

**语法规则怎么写（BNF）**：用类似「产生式」描述语言结构。例如：

~~~
expr  ->  assign
assign->  logor ("=" assign)?
logor ->  logand ("||" logand)*
logand->  eq ("&&" eq)*
eq    ->  rel ("==" rel | "!=" rel)*
rel   ->  add ("<" add | "<=" add | ">" add | ">=" add)*
add   ->  mul ("+" mul | "-" mul)*
mul   ->  unary ("*" unary | "/" unary | "%" unary)*
unary ->  ("-" | "!" | "~") unary | primary
primary-> "(" expr ")" | 数字 | 标识符 | 字符串
~~~

其中 \`*\` 表示「重复 0 次以上」，\`?\` 表示「可选」，\`|\` 表示「或」。

**递归下降解析**：为每个产生式写一个函数，函数之间互相调用。这就是课程阶段 3 的核心。

**运算符优先级**用「分层」实现：每层只管一种优先级，高层调用低层。上面 BNF 从低到高是：赋值 → 逻辑或 → 逻辑与 → 相等 → 比较 → 加减 → 乘除模 → 一元 → 括号/原子。

**结合性**：本项目都是「左结合」——\`1 - 2 - 3\` 按 \`(1-2)-3\` 理解。在循环里不断向左合并即可。

**语句**（阶段 3 还要解析）：

~~~
return 表达式? ;
if (表达式) 语句 (else 语句)?
while (表达式) 语句
for (表达式?; 表达式?; 表达式?) 语句
int 标识符 (= 表达式)? ;     // 局部变量声明
{ 语句* }                     // 块
表达式 ;                      // 表达式语句
~~~

**打印 AST**：递归遍历，每个节点打印成 S 表达式，测试用「去掉所有空白再比较」来判断对错。`,
  },

  {
    id: 'semantics',
    title: '6. 语义分析与符号表',
    content: `语法正确 ≠ 有意义。\`return x;\` 语法没错，但如果 x 从没声明过，就是**语义错误**。语义分析就是检查这类「意义」问题。

**本项目做两件事：**

1. **检测未定义变量**：用了没声明过的变量 → 报错 \`undefined variable\`。
2. **检测重复声明**：同一作用域里重复声明同名变量 → 报错 \`redeclared\`。

**符号表**：记录「变量名 → 信息」的映射。本项目变量只有 int 类型，所以主要记录**变量名 → 栈偏移**。

**作用域（scope）**：变量只在它声明的 \`{}\` 内可见。实现方式：每进入一个 \`{}\` 压入一层新的符号表，离开时弹出。

~~~
int main() {
  int a = 1;      // 外层作用域，声明 a
  {               // 进入新作用域
    int a = 2;    // 这是另一个 a，合法（不同作用域）
  }               // 离开，内层 a 消失
  return a;       // 这里的 a 是外层的 = 1
}
~~~

**查找规则**：用变量时，从最内层作用域向外逐层查找，第一个匹配的就是它。

**数据结构**：C 里可以用链表（每个作用域一个链表头，层层链接）；C++ 里用 \`std::vector<std::map<std::string,int>>\` 最省事——vector 表示作用域栈，map 存「名字→偏移」。

**栈偏移**：给每个变量分配一个「相对于栈底的偏移」（如 8、16、24…），供阶段 5 代码生成时读写局部变量用。`,
  },

  {
    id: 'codegen',
    title: '7. 代码生成 Codegen',
    content: `**代码生成**把 AST 翻译成 x86-64 汇编。本项目采用**栈机模型**——用一个「栈」保存中间结果，思路简单清晰。

**整体结构**（一个函数）：

~~~
.text
.globl main
main:
  push %rbp          # 保存旧栈底
  movq %rsp, %rbp    # 新栈底 = 当前栈顶
  subq $N, %rsp      # 给局部变量留 N 字节空间
  ... 函数体 ...
.L.return:
  movq %rbp, %rsp    # 恢复栈顶
  pop %rbp           # 恢复旧栈底
  ret                # 返回
~~~

**表达式求值**——结果放在 \`%eax\`：

- 数字：\`movl $42, %eax\`
- 变量：\`movl -8(%rbp), %eax\`（从栈上读）
- 二元运算：先算左操作数 → \`push %rax\` 保存 → 算右操作数 → 把结果挪到 \`%edi\` → \`pop %rax\` 恢复左操作数 → 运算

~~~
# 计算 2 + 3 * 4
movl $2, %eax        # 左 = 2
push %rax            # 保存 2
movl $3, %eax        # 右左边 3
push %rax            # 保存 3
movl $4, %eax        # 4
movl %eax, %edi      # 右 = 4
pop %rax             # 取回 3
imull %edi, %eax     # 3 * 4 = 12
movl %eax, %edi      # 右 = 12
pop %rax             # 取回 2
addl %edi, %eax      # 2 + 12 = 14
~~~

**控制流**用「标签 + 跳转」：

~~~
# if (cond) A; else B;
  ... 求 cond 到 %eax ...
  cmpl $0, %eax
  je .L.else        # 若为 0，跳到 else
  ... A ...
  jmp .L.end
.L.else:
  ... B ...
.L.end:
~~~

**比较**：\`cmp\` 之后用 \`setXX\` 得到 0/1：

~~~
cmpl %edi, %eax   # 比较 %eax 和 %edi
sete %al          # 相等则 %al=1，否则 0
movzbl %al, %eax  # 扩展到 32 位
~~~

**除法**：先 \`cqto\` 符号扩展，再 \`idivl\`：

~~~
cqto          # %edx:%eax 组成 64 位符号数
idivl %edi    # 商在 %eax，余数在 %edx
~~~

**短路求值**（阶段 6）：\`a && b\` 若 a 为假就跳过 b，用跳转实现。`,
  },

  {
    id: 'x86',
    title: '8. x86-64 汇编速查',
    content: `本项目用的是 **x86-64 AT&T 语法**（gcc/g++ 默认输出这种）。注意它与 Intel 语法**操作数顺序相反**。

**常用寄存器**：

| 寄存器 | 用途 |
|---|---|
| %rax / %eax | 返回值 / 通用计算（%eax 是低 32 位） |
| %rbx, %rcx, %rdx | 通用 |
| %rsp | 栈顶指针 |
| %rbp | 栈底（栈帧基址） |
| %rdi, %rsi | 函数参数（本项目 main 无参数） |

**常用指令**（AT&T 语法：\`指令 源, 目标\`）：

| 指令 | 含义 | 例子 |
|---|---|---|
| movl | 移动 32 位 | \`movl $42, %eax\`（立即数 42 → eax） |
| addl / subl | 加 / 减 | \`addl %edi, %eax\`（eax += edi） |
| imull | 有符号乘 | \`imull %edi, %eax\` |
| idivl | 有符号除 | 见上文除法 |
| negl | 取负 | \`negl %eax\` |
| notl | 按位取反 | \`notl %eax\` |
| cmpl | 比较 | \`cmpl %edi, %eax\`（计算 eax - edi 设置标志） |
| push / pop | 压栈 / 弹栈 | \`push %rax\` / \`pop %rax\` |
| jmp | 无条件跳转 | \`jmp .L.end\` |
| je / jne | 相等 / 不等则跳 | \`je .L.else\` |
| jl / jle / jg / jge | 小于/小于等于/大于/大于等于 | \`jl .L.loop\` |
| sete / setne / setl / setg 等 | 按比较结果置 0/1 到 %al | \`sete %al\` |
| movzbl | 0 扩展字节到 32 位 | \`movzbl %al, %eax\` |
| cqto | 符号扩展 %eax 到 %edx:%eax | \`cqto\` |
| call / ret | 调用 / 返回 | \`ret\` |

**立即数与内存**：

- 立即数：\`$42\`（带美元符号）
- 寄存器：\`%eax\`（带百分号）
- 内存：\`-8(%rbp)\`（栈上 rbp 偏移 -8 处）

**退出码**：程序 \`return N;\` 时，N 会被放进 \`%eax\`，进程退出码就是 \`%eax\` 的低 8 位（0~255）。

**大小后缀**：\`b\`=字节(8位)、\`w\`=字(16位)、\`l\`=长字(32位)、\`q\`=四字(64位)。本项目局部变量都是 32 位 int，所以用 \`l\` 后缀。`,
  },

  {
    id: 'toolchain',
    title: '9. 从汇编到可执行文件（工具链）',
    content: `你只负责生成**汇编文本**，剩下的交给现成工具链：

~~~
源代码 .c/.cpp ──你的 mycc──▶ 汇编 .s ──汇编器 as──▶ 目标文件 .o ──链接器 ld──▶ 可执行文件 .exe
~~~

**gcc / g++ 是什么？** 它其实是「编译器驱动器」——一条命令替你调用预处理器、编译器、汇编器、链接器。

- C 源码用 \`gcc\`；C++ 源码用 \`g++\`。
- 常用：\`gcc test.s -o test.exe\` 会把汇编 test.s 汇编+链接成 test.exe。

**本项目测试的完整流程**（build-runner 自动做）：

~~~
1. 你的 mycc 把 test.c 编译成 test.s（汇编文本）
2. gcc 把 test.s 汇编+链接成 test.exe
3. 运行 test.exe，读取它的退出码
4. 比对退出码是否等于期望值
~~~

**退出码**：程序正常返回 0 表示成功，非 0 表示出错。测试用例就是靠「退出码」和「输出文本」判断对错。

**编译 vs 汇编 vs 链接**：

- **编译**：高级语言 → 汇编
- **汇编**：汇编 → 机器码（目标文件 .o）
- **链接**：把多个目标文件 + 库 组合成可执行文件

**为什么要链接**：你的程序可能用到库函数（如 printf），链接器负责把这些外部函数「接上」。本项目 mycc 生成的汇编很简单（只有 main，不调库），所以链接很简单。`,
  },

  {
    id: 'datastructures',
    title: '10. 数据结构与算法基础',
    content: `写编译器会用到几个基础数据结构，这里列最常用的。

**链表（Linked List）**：一串节点，每个节点指向下一个。适合「不确定有多少个、要动态增删」的场景，比如 token 流。

~~~
C:  struct Token { TokenKind kind; struct Token* next; ... };
C++: std::vector<Token> 通常更省事（本项目参考实现就用 vector）
~~~

**栈（Stack）**：后进先出（LIFO）。编译器里两处用到栈：
1. **作用域栈**（阶段 4）：进入 \`{}\` 压入一层符号表，离开弹出。
2. **求值栈**（阶段 5 代码生成）：算表达式时保存中间结果（push/pop）。

**树（Tree）**：AST 就是一棵树。每个节点有若干子节点。

**哈希表 / map**：快速「按名字查找」。符号表查找变量就是按名字查，C++ 用 \`std::map\` 或 \`std::unordered_map\` 最方便。

**递归（Recursion）**：函数调用自身。编译器里到处都是递归：
- 递归下降解析：expr() 调 mul()，mul() 调 unary()...
- AST 遍历：打印、代码生成都是递归遍历树

**递归的本质**：把大问题拆成结构相同的子问题。写递归时先想清楚「最小情况（终止条件）」和「怎么缩小问题」。

**指针 / 引用**（C 用指针，C++ 用引用或智能指针）：
- 指向内存地址，用于共享和避免拷贝。
- 树节点、链表节点之间的连接就靠指针/引用。

**动态内存**：
- C：\`malloc\` / \`free\`
- C++：\`new\` / \`delete\`，或直接用 \`std::string\`/\`std::vector\` 自动管理

> 建议：用 C++ 实现时，优先用 \`std::string\`、\`std::vector\`、\`std::unique_ptr\`，避免手写内存管理，能少踩很多坑。`,
  },

  {
    id: 'debugging',
    title: '11. 调试技巧',
    content: `编译器是最难调试的程序之一（你在「造工具」），这里给几条实用心法。

**1. 最小可复现**：遇到 bug，先构造一个**最小**的出错输入（越短越好），再针对性排查。

**2. 分层验证**：每完成一个阶段，立刻用「编译并测试」验证，别攒一堆再改。课程路线就是这么设计的。

**3. 打印调试**：在关键位置打印中间状态。比如词法器打印每个 token，解析器打印 AST，代码生成打印每条汇编。\`printf\` / \`std::cerr\` 是最朴素但最好用的调试器。

**4. 用 -t 和 -a 分阶段排查**：

~~~
# 先看词法对不对
mycc -t test.c
# 词法对了再看语法树
mycc -a test.c
# 语法对了再看汇编
mycc test.c
~~~

**5. 常见 bug 清单**：

- 词法：忘了跳过注释/空白、多字符运算符顺序不对、EOF 没加
- 语法：优先级分层错了、\`return\` 没吃分号、忘了递归
- 语义：作用域没弹栈、符号表查找方向反了
- 代码生成：寄存器/栈没配对、跳转标签写错、除法没 \`cqto\`

**6. 看汇编对不对**：把你的输出和 \`gcc -S test.c\` 的输出对比思路（不必逐字节一致，看结构）。

**7. 二分定位**：注释掉一半代码，看 bug 还在不在，快速缩小范围。

**8. 善用 AI 导师**：卡住时用「💡 给点提示」「🔍 评判我的代码」，带上完整上下文（可用 📎 选择相关文件）问它。`,
  },
];
